export class App {
    /**
     * Initialize the app instance.
     *
     * @param  {string}  id
     * @param  {string}  key
     * @param  {string}  secret
     */
    constructor(public id: string, public key: string, public secret: string) {
        //
    }
}
